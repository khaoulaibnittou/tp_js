/**
 *
 *
 * @param {number} a premier nombre
 * @param {number} b deuxieme nombre 
 * @return {number} resultat
 */
function ajouter(a, b) {
    return a + b;
}
const ajoute = (a, b) => a + b;
console.log(ajoute(3, 5)); 